apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: http01-clusterissuer
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: std-ext-016-02@praktikum-services.ru 
    privateKeySecretRef:
      name: acme-http01-private-key
    solvers:
      - http01:
          ingress:
            class: nginx
